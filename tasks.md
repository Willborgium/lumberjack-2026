# To Do

- (none)

# Completed

1. Make sure IDisposable is properly implemented across all classes that warrant it — `2d92393`
2. Implement a simple effect/render state stack to manage renderable-specific GPU state changes — `2d92393`
3. Implement basic action mapping system that translates inputs (like keyboard buttons or mouse input) to actions — `2d92393`
4. Make resource manager thread safe (cache synchronization scope) — `64e0c48`
